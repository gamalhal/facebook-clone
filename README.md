مشروع Mini-Facebook Clone
هذا المشروع هو تطبيق ويب مصغر شبيه بفيسبوك، تم بناؤه من الصفر باستخدام React و Firebase. يوفر التطبيق الميزات الأساسية لشبكة تواصل اجتماعي، بما في ذلك مصادقة المستخدمين، وإنشاء المنشورات، والتفاعل معها عبر الإعجابات والتعليقات، بالإضافة إلى صفحات ملفات شخصية ديناميكية.

✨ الميزات الرئيسية
نظام مصادقة متكامل: تسجيل مستخدمين جدد، تسجيل الدخول، وإدارة الجلسات باستخدام Firebase Authentication.

إدارة المنشورات (CRUD): يمكن للمستخدمين إنشاء منشورات، قراءتها، تحديثها، وحذفها.

التفاعل الاجتماعي:

نظام إعجابات (Likes) مع تحديث فوري للعدد.

نظام تعليقات (Comments) لكل منشور مع عرض فوري للتعليقات الجديدة.

التوجيه الديناميكي: صفحات ملف شخصي (/profile/:userId) لكل مستخدم تعرض منشوراته فقط.

حماية المسارات: لا يمكن الوصول إلى الصفحات الرئيسية وصفحات الملفات الشخصية إلا للمستخدمين المسجلين.

واجهة مستخدم حديثة: تصميم نظيف ومتجاوب باستخدام Tailwind CSS.

تحديثات في الوقت الفعلي: يتم تحديث جميع البيانات (المنشورات، الإعجابات، التعليقات) فورًا عبر جميع العملاء المتصلين بفضل Firestore Real-time Listeners.

تجربة مستخدم محسنة: مؤشرات تحميل (Loading Spinners) تظهر أثناء جلب البيانات.

🛠️ التقنيات المستخدمة
الواجهة الأمامية (Frontend):

React 18 (مع Hooks)

Vite (كأداة بناء وتشغيل)

React Router v6 (للتوجيه من جانب العميل)

Tailwind CSS (للتصميم)

الواجهة الخلفية (Backend - BaaS):

Firebase v9:

Authentication (لإدارة المستخدمين)

Firestore (كقاعدة بيانات NoSQL في الوقت الفعلي)

لغة البرمجة: JavaScript (ES6+)

🚀 كيفية تشغيل المشروع محليًا
لتشغيل نسخة من هذا المشروع على جهازك، اتبع الخطوات التالية:

1. المتطلبات الأساسية
تأكد من أن لديك Node.js (إصدار 16 أو أحدث) مثبتًا على جهازك.

2. استنساخ المستودع (Clone)
git clone [https://github.com/your-username/facebook-clone.git](https://github.com/your-username/facebook-clone.git)
cd facebook-clone

3. تثبيت الاعتماديات (Dependencies)
npm install

4. إعداد Firebase
اذهب إلى وحدة تحكم Firebase وأنشئ مشروعًا جديدًا.

قم بتفعيل خدمتي Authentication (مع مزود البريد الإلكتروني/كلمة المرور) و Firestore Database (في وضع الاختبار).

من إعدادات المشروع، قم بإنشاء "تطبيق ويب" جديد وانسخ كائن firebaseConfig.

5. إعداد متغيرات البيئة
في جذر المشروع، أنشئ ملفًا جديدًا باسم .env.local.

الصق مفاتيح Firebase الخاصة بك في هذا الملف بالصيغة التالية:

VITE_API_KEY="YOUR_API_KEY"
VITE_AUTH_DOMAIN="YOUR_AUTH_DOMAIN"
VITE_PROJECT_ID="YOUR_PROJECT_ID"
VITE_STORAGE_BUCKET="YOUR_STORAGE_BUCKET"
VITE_MESSAGING_SENDER_ID="YOUR_MESSAGING_SENDER_ID"
VITE_APP_ID="YOUR_APP_ID"

6. تشغيل خادم التطوير
npm run dev

يجب أن يعمل التطبيق الآن على http://localhost:5173 (أو منفذ آخر يظهر في الطرفية).

🔮 تحسينات مستقبلية محتملة
رفع الصور: السماح للمستخدمين برفع صور للملف الشخصي وصور مع المنشورات باستخدام Firebase Storage.

الإشعارات: إرسال إشعارات للمستخدمين عند تلقي إعجاب أو تعليق.

التمرير اللانهائي (Infinite Scroll): تحميل المزيد من المنشورات تلقائيًا عند وصول المستخدم إلى أسفل الصفحة.

البحث: إضافة شريط بحث للعثور على المستخدمين الآخرين.